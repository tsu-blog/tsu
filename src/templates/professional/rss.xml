<?xml version"1.0" ?>
<rss version="2.0">
  <channel>
    <title>{{title}}</title>
    <link>{{homepage}}</link>
    <description>{{description}}</description>
    <language>en-us</language>
    {% for post in posts %}
    <item>
      <title>{{post['title']}}</title>
      <link>/post/{{post['id']}}</link>
      <description>
        {{post['body']}}
      </description>
      <dc:creator>{{author}}</dc:creator>
      <dc:date>{{post['created_at'].strftime("%Y-%m-%d")}}</dc:date>
    </item>
    {% endfor %}
  </channel>

